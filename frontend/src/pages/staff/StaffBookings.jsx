import Bookings from '../admin/Bookings';

const StaffBookings = () => (
  <Bookings
    pageTitle="Manage Bookings"
    pageSubtitle="All reservations from the database — search, create, check-in/out."
  />
);

export default StaffBookings;
