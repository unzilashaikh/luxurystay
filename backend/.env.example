NODE_ENV=development
PORT=5000

# 1) Start database:  docker compose up -d   (from HM folder)
# 2) Then start API:   npm run dev
MONGO_URI=mongodb://127.0.0.1:27017/luxurystay

# First run only — creates admin/housekeeping logins when DB is empty
SEED_DEV_USERS=true
